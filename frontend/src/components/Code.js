const Code = ({children}) => (
  <code
    style={{
      padding: '3px 6px',
      borderRadius: '4px',
      backgroundColor: 'rgba(0,0,0,.07)',
    }}>
    {children}
  </code>
);

export default Code;
